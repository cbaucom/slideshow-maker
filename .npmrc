# Allow esbuild to run its install script

# Disable pnpm v10's supply-chain age policy — we use recently-published packages
# (remotion, playwright-cli) that would otherwise fail the default 24h cutoff.
minimum-release-age=0
