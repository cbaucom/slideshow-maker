# Jamendo API client ID — required for the "Find Music" panel in dev and prod builds.
# Get one at https://devportal.jamendo.com/
# Local: copy to .env.local. Production: set in Netlify → Site configuration → Environment variables.
VITE_JAMENDO_CLIENT_ID=
